# Gateway Bootstrap 生产环境配置
spring:
  application:
    name: gateway
  cloud:
    nacos:
      # 彻底禁用 Nacos 配置中心
      config:
        enabled: false
        server-addr: none
      # 禁用服务发现
      discovery:
        enabled: false
