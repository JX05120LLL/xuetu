# 学习服务 - 生产环境配置
spring:
  application:
    name: learning-service
    
  # 数据源配置
  datasource:
    url: jdbc:mysql://8.141.106.92:3306/xuetu_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  # Redis配置
  redis:
    host: 8.141.106.92
    port: 6379
    password: 
    database: 0
    
  # 启用 Nacos
  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: 8.141.106.92:8848
        namespace: xuetu-prod
        group: DEFAULT_GROUP
        ip: 112.126.85.23  # 使用宿主机IP
        port: 8044         # 使用宿主机端口
      config:
        enabled: false
        import-check:
          enabled: false  # 禁用强制导入检查

# 日志配置
logging:
  level:
    com.star.learning: INFO
    root: INFO
  file:
    path: /var/log/xuetu/learning-service
    name: /var/log/xuetu/learning-service/learning-service.log
