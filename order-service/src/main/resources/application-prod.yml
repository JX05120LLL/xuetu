# 订单服务 - 生产环境配置
spring:
  application:
    name: order-service
    
  # 数据源配置
  datasource:
    url: jdbc:mysql://8.141.106.92:3306/xuetu_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  # Redis配置
  redis:
    host: 8.141.106.92
    port: 6379
    password: 
    database: 0
    
  # 启用 Nacos
  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: 8.141.106.92:8848
        namespace: xuetu-prod
        group: DEFAULT_GROUP
        ip: 8.140.224.117  # 使用宿主机IP（服务器3）
        port: 8033         # 使用宿主机端口
      config:
        enabled: false
        import-check:
          enabled: false  # 禁用强制导入检查

# 日志配置
logging:
  level:
    com.star.order: INFO
    root: INFO
  file:
    path: /var/log/xuetu/order-service
    name: /var/log/xuetu/order-service/order-service.log
