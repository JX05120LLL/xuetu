# 🎯 学途智慧教育平台 - 完整开发规划与实施方案

> 制定时间: 2025年10月2日  
> 规划周期: 8周（2个月）  
> 目标: 完成全栈开发、测试、部署上线

---

## 📊 当前项目状态评估

### ✅ 已完成模块
| 服务 | 完成度 | 状态 | 说明 |
|------|--------|------|------|
| 公共模块 | 100% | ✅ 稳定 | 统一异常、结果、工具类完整 |
| 网关服务 | 100% | ✅ 稳定 | 路由、跨域、JWT完整 |
| 用户服务 | 60% | 🔄 待完善 | 注册登录完成，角色权限待开发 |
| 课程服务 | 95% | ✅ 基本完成 | CRUD完整，需完善文件上传 |
| 订单服务 | 90% | ✅ 基本完成 | 模拟支付完成，待集成真实支付 |
| 学习服务 | 90% | ✅ 基本完成 | 学习记录完成，部分TODO待处理 |
| AI服务 | 0% | ❌ 未开始 | **核心亮点，优先级最高** |
| 管理服务 | 0% | ❌ 未开始 | 后台管理功能待开发 |

### ⚠️ 已知问题清单

#### 1. 代码中的TODO项
```java
// learning-service
- LearningRecordServiceImpl.java (Line 181): 实现用户学习排名逻辑
- LearningRecordServiceImpl.java (Line 187): 实现连续学习天数计算逻辑
- NoteController.java (Line 166): 从JWT Token中解析用户ID
- LearningRecordController.java (Line 135): 从JWT Token中解析用户ID

// course-service  
- CommentServiceImpl.java (Line 324, 345): 设置用户是否已点赞状态
```

#### 2. 功能缺失项
- ❌ 文件上传功能（课程封面、视频、用户头像）
- ❌ 图片存储服务（本地或OSS）
- ❌ 视频播放服务（集成或自建）
- ❌ 真实支付集成（支付宝/微信）
- ❌ 消息通知功能（站内信、邮件）
- ❌ 数据统计看板
- ❌ 日志审计功能
- ❌ 系统监控告警

#### 3. 测试缺失
- ❌ 单元测试覆盖率 0%
- ❌ 集成测试 0%
- ❌ 接口测试脚本
- ❌ 性能测试

#### 4. 前端完全缺失
- ❌ 用户端前端（学生/讲师）
- ❌ 后台管理前端（管理员）
- ❌ 移动端H5（可选）

---

## 🎯 完整开发计划（8周）

### 📅 第1-2周：后端核心功能完善

#### Week 1: 修复现有问题 + AI服务框架（5-7天）

**Day 1-2: 修复已知问题**
- [ ] **修复TODO项**
  ```bash
  1. LearningRecordServiceImpl - 实现学习排名和连续天数
  2. 所有Controller - JWT解析用户ID统一处理
  3. CommentServiceImpl - 实现点赞状态查询
  ```

- [ ] **完善用户认证**
  ```java
  1. 创建统一的JWT工具类
  2. 网关添加用户信息到请求头
  3. 各服务统一获取当前用户
  4. 添加@LoginRequired注解（可选）
  ```

- [ ] **代码质量提升**
  ```bash
  1. 统一异常处理完善
  2. 统一日志规范
  3. 接口参数校验增强
  4. 业务逻辑优化
  ```

**Day 3-5: AI服务开发（第一阶段）**
- [ ] **创建AI服务基础框架**
  ```bash
  ai-service/
  ├── config/
  │   ├── Knife4jConfig.java
  │   └── AIClientConfig.java
  ├── controller/
  │   ├── AIChatController.java          # AI对话
  │   └── LearningAssistantController.java # 学习助手
  ├── service/
  │   ├── AIChatService.java
  │   └── LearningAssistantService.java
  ├── client/
  │   └── BaiduAIClient.java              # 百度文心一言客户端
  ├── dto/
  │   ├── ChatRequest.java
  │   ├── ChatResponse.java
  │   └── ChatHistoryDTO.java
  ├── entity/
  │   └── ChatHistory.java                # 对话历史
  └── mapper/
      └── ChatHistoryMapper.java
  ```

- [ ] **集成百度文心一言API**
  ```yaml
  # application.yml
  ai:
    baidu:
      api-key: your_api_key
      secret-key: your_secret_key
      endpoint: https://aip.baidubce.com/rpc/2.0/ai_custom/v1/wenxinworkshop/chat/completions
  ```

- [ ] **实现核心接口**
  ```java
  1. POST /ai/chat/ask - 基础问答
  2. GET /ai/chat/history - 对话历史
  3. POST /ai/assistant/analyze - 学习分析
  4. POST /ai/assistant/recommend - 课程推荐
  ```

**Day 6-7: 文件上传服务**
- [ ] **创建文件上传Controller**
  ```java
  // common-service 或 独立的 file-service
  @RestController
  @RequestMapping("/file")
  public class FileUploadController {
      
      @PostMapping("/upload/image")
      public R<String> uploadImage(@RequestParam MultipartFile file) {
          // 上传图片（课程封面、头像）
      }
      
      @PostMapping("/upload/video")  
      public R<String> uploadVideo(@RequestParam MultipartFile file) {
          // 上传视频（课程视频）
      }
      
      @DeleteMapping("/delete")
      public R<Boolean> deleteFile(@RequestParam String fileUrl) {
          // 删除文件
      }
  }
  ```

- [ ] **存储方案选择**
  ```yaml
  方案1: 本地存储（开发环境）
    - 优点: 简单快速，无成本
    - 缺点: 不适合生产环境
    - 建议: 用于开发测试
    
  方案2: 阿里云OSS（生产推荐）
    - 优点: 稳定可靠，成本低
    - 缺点: 需要配置
    - 建议: 用于生产部署
    
  方案3: MinIO自建（可选）
    - 优点: 开源免费，自主可控
    - 缺点: 需要部署维护
    - 建议: 团队项目使用
  ```

**交付物**:
- ✅ 所有TODO项已修复
- ✅ AI服务基础框架完成
- ✅ AI基础问答功能可用
- ✅ 文件上传功能完成

---

#### Week 2: 角色权限 + 管理服务（5-7天）

**Day 1-3: 用户角色权限管理**
- [ ] **创建角色管理模块**
  ```java
  // user-service
  @RestController
  @RequestMapping("/role")
  public class RoleController {
      
      @PostMapping                         // 创建角色
      @GetMapping("/list")                 // 角色列表
      @GetMapping("/{id}")                 // 角色详情
      @PutMapping("/{id}")                 // 更新角色
      @DeleteMapping("/{id}")              // 删除角色
      
      @PostMapping("/{roleId}/permissions") // 分配权限
      @GetMapping("/{roleId}/permissions")  // 查询角色权限
  }
  ```

- [ ] **创建权限管理模块**
  ```java
  @RestController
  @RequestMapping("/permission")
  public class PermissionController {
      
      @PostMapping                         // 创建权限
      @GetMapping("/list")                 // 权限列表
      @GetMapping("/tree")                 // 权限树
      @PutMapping("/{id}")                 // 更新权限
      @DeleteMapping("/{id}")              // 删除权限
  }
  ```

- [ ] **实现RBAC权限控制**
  ```java
  // 网关层权限拦截
  @Component
  public class PermissionFilter implements GlobalFilter {
      
      @Override
      public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {
          // 1. 从JWT获取用户角色
          // 2. 查询角色权限
          // 3. 验证是否有接口访问权限
          // 4. 放行或拒绝
      }
  }
  ```

**Day 4-7: 管理服务开发（后台管理）**
- [ ] **创建admin-service基础框架**
  ```bash
  admin-service/
  ├── controller/
  │   ├── AdminUserController.java      # 用户管理
  │   ├── AdminCourseController.java    # 课程管理
  │   ├── AdminOrderController.java     # 订单管理
  │   ├── AdminStatisticsController.java # 数据统计
  │   └── AdminSystemController.java    # 系统设置
  ├── service/
  │   ├── AdminUserService.java
  │   ├── AdminCourseService.java
  │   └── AdminStatisticsService.java
  └── feign/
      ├── UserServiceClient.java         # 调用用户服务
      ├── CourseServiceClient.java       # 调用课程服务
      └── OrderServiceClient.java        # 调用订单服务
  ```

- [ ] **实现核心管理功能**
  ```java
  1. 用户管理
     - 用户列表、详情、禁用/启用
     - 角色分配
     - 用户数据统计
  
  2. 课程管理（审核）
     - 课程审核（通过/拒绝）
     - 课程推荐设置
     - 课程下架
  
  3. 订单管理
     - 订单列表、详情
     - 退款处理
     - 订单统计
  
  4. 数据统计
     - 用户增长统计
     - 课程销售统计
     - 学习数据统计
     - 收入统计
  ```

**交付物**:
- ✅ 角色权限管理功能完整
- ✅ RBAC权限控制实现
- ✅ 后台管理服务基础功能完成

---

### 📅 第3-4周：前端开发（用户端）

#### Week 3: 用户端前端基础（5-7天）

**技术选型**:
```yaml
框架: Vue 3 + Vite
UI库: Element Plus / Ant Design Vue
路由: Vue Router 4
状态管理: Pinia
HTTP: Axios
图表: ECharts
其他: 
  - vueuse (组合式工具库)
  - dayjs (日期处理)
  - md-editor-v3 (Markdown编辑器)
```

**Day 1-2: 项目初始化**
- [ ] **创建前端项目**
  ```bash
  # 使用Vite创建
  npm create vite@latest xuetu-frontend -- --template vue
  cd xuetu-frontend
  
  # 安装依赖
  npm install element-plus axios pinia vue-router
  npm install echarts vueuse dayjs
  ```

- [ ] **项目结构**
  ```bash
  xuetu-frontend/
  ├── public/
  ├── src/
  │   ├── api/                    # API接口
  │   │   ├── user.js
  │   │   ├── course.js
  │   │   ├── order.js
  │   │   └── learning.js
  │   ├── assets/                 # 静态资源
  │   ├── components/             # 公共组件
  │   │   ├── Header.vue
  │   │   ├── Footer.vue
  │   │   └── CourseCard.vue
  │   ├── views/                  # 页面
  │   │   ├── Home.vue           # 首页
  │   │   ├── Login.vue          # 登录
  │   │   ├── Register.vue       # 注册
  │   │   ├── CourseList.vue     # 课程列表
  │   │   ├── CourseDetail.vue   # 课程详情
  │   │   ├── Learning.vue       # 学习页面
  │   │   ├── UserCenter.vue     # 个人中心
  │   │   └── AIChat.vue         # AI助手
  │   ├── router/                 # 路由
  │   ├── store/                  # 状态管理
  │   ├── utils/                  # 工具函数
  │   ├── App.vue
  │   └── main.js
  ├── package.json
  └── vite.config.js
  ```

- [ ] **配置Axios + 路由守卫**
  ```javascript
  // utils/request.js
  import axios from 'axios'
  
  const request = axios.create({
    baseURL: 'http://localhost:8080/api',
    timeout: 10000
  })
  
  // 请求拦截器 - 添加Token
  request.interceptors.request.use(config => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  })
  
  // 响应拦截器 - 统一处理
  request.interceptors.response.use(
    response => response.data,
    error => {
      // 统一错误处理
      return Promise.reject(error)
    }
  )
  ```

**Day 3-7: 核心页面开发**
- [ ] **登录注册页面**
  ```vue
  <!-- views/Login.vue -->
  - 用户名/密码登录
  - 表单验证
  - 登录成功存储Token
  - 跳转到首页
  ```

- [ ] **首页**
  ```vue
  <!-- views/Home.vue -->
  - 顶部导航栏（搜索、分类、用户信息）
  - Banner轮播图
  - 热门课程展示
  - 课程分类
  - 推荐课程
  - 底部信息
  ```

- [ ] **课程列表页**
  ```vue
  <!-- views/CourseList.vue -->
  - 侧边分类筛选
  - 课程搜索
  - 难度/价格筛选
  - 课程卡片展示
  - 分页
  ```

- [ ] **课程详情页**
  ```vue
  <!-- views/CourseDetail.vue -->
  - 课程基本信息
  - 讲师信息
  - 课程目录（章节+课时）
  - 课程评价
  - 购买按钮
  ```

**交付物**:
- ✅ 前端项目框架搭建完成
- ✅ 用户端核心页面完成（首页、登录、课程列表、课程详情）

---

#### Week 4: 用户端前端完善（5-7天）

**Day 1-3: 学习相关页面**
- [ ] **学习页面**
  ```vue
  <!-- views/Learning.vue -->
  - 视频播放器
  - 课程目录侧边栏
  - 学习进度显示
  - 笔记功能
  - 评论区
  - 下一课时按钮
  ```

- [ ] **个人中心**
  ```vue
  <!-- views/UserCenter.vue -->
  - 侧边导航（我的课程、我的订单、学习记录、个人信息）
  - 我的课程列表
  - 学习进度统计
  - 订单列表
  - 个人信息编辑
  ```

**Day 4-5: AI助手页面**
- [ ] **AI聊天页面**
  ```vue
  <!-- views/AIChat.vue -->
  - 聊天对话框
  - 消息列表（用户/AI）
  - 输入框
  - 发送按钮
  - 历史记录
  - 快捷问题推荐
  ```

**Day 6-7: 优化与美化**
- [ ] **UI优化**
  ```bash
  1. 响应式布局（适配PC和平板）
  2. 加载动画
  3. 骨架屏
  4. 错误页面（404、500）
  5. 空状态页面
  ```

- [ ] **用户体验优化**
  ```bash
  1. 图片懒加载
  2. 路由懒加载
  3. 防抖节流
  4. 错误提示优化
  5. 成功反馈
  ```

**交付物**:
- ✅ 用户端前端完整功能
- ✅ AI助手界面完成
- ✅ 响应式布局完成

---

### 📅 第5-6周：后台管理前端 + 功能完善

#### Week 5: 后台管理前端（5-7天）

**Day 1-2: 后台管理项目初始化**
- [ ] **创建后台管理项目**
  ```bash
  # 方案1: 使用现成Admin模板（推荐）
  git clone https://github.com/pure-admin/vue-pure-admin
  # 或
  git clone https://github.com/vbenjs/vue-vben-admin
  
  # 方案2: 自己搭建
  npm create vite@latest xuetu-admin -- --template vue
  ```

- [ ] **后台管理结构**
  ```bash
  xuetu-admin/
  ├── src/
  │   ├── api/
  │   │   ├── admin/
  │   │   │   ├── user.js        # 用户管理API
  │   │   │   ├── course.js      # 课程管理API
  │   │   │   ├── order.js       # 订单管理API
  │   │   │   └── statistics.js  # 统计API
  │   ├── views/
  │   │   ├── dashboard/          # 数据看板
  │   │   ├── user/               # 用户管理
  │   │   ├── course/             # 课程管理
  │   │   ├── order/              # 订单管理
  │   │   ├── system/             # 系统设置
  │   │   │   ├── role.vue       # 角色管理
  │   │   │   └── permission.vue # 权限管理
  │   └── layout/                 # 布局组件
  ```

**Day 3-7: 后台管理核心页面**
- [ ] **数据看板**
  ```vue
  <!-- views/dashboard/index.vue -->
  - 今日数据概览（用户数、订单数、收入）
  - 用户增长趋势图（ECharts）
  - 课程销售排行
  - 最近订单列表
  - 系统通知
  ```

- [ ] **用户管理**
  ```vue
  <!-- views/user/list.vue -->
  - 用户列表（表格）
  - 搜索筛选
  - 用户详情弹窗
  - 禁用/启用用户
  - 角色分配
  ```

- [ ] **课程管理**
  ```vue
  <!-- views/course/list.vue -->
  - 课程列表
  - 课程审核（通过/拒绝）
  - 课程推荐设置
  - 课程编辑
  - 课程上/下架
  ```

- [ ] **订单管理**
  ```vue
  <!-- views/order/list.vue -->
  - 订单列表
  - 订单详情
  - 退款处理
  - 订单统计
  ```

- [ ] **角色权限管理**
  ```vue
  <!-- views/system/role.vue -->
  - 角色列表
  - 角色添加/编辑
  - 权限分配（树形结构）
  
  <!-- views/system/permission.vue -->
  - 权限列表（树形）
  - 权限添加/编辑
  ```

**交付物**:
- ✅ 后台管理前端完成
- ✅ 数据看板可视化完成
- ✅ 管理功能完整

---

#### Week 6: 功能完善与优化（5-7天）

**Day 1-2: AI服务完善（第二阶段）**
- [ ] **智能学习助手**
  ```java
  1. 学习数据分析
     - 分析用户学习进度
     - 识别薄弱知识点
     - 生成学习建议
  
  2. 智能课程推荐
     - 基于学习历史推荐
     - 基于知识图谱推荐
     - 个性化学习路径
  
  3. 课程内容问答
     - 课程知识点解答
     - 习题讲解
     - 疑难问题解答
  ```

**Day 3-4: 消息通知功能**
- [ ] **站内消息**
  ```java
  // 创建notification-service（可选，也可放在user-service）
  @RestController
  @RequestMapping("/notification")
  public class NotificationController {
      
      @GetMapping("/my")              // 我的消息
      @PostMapping("/read/{id}")      // 标记已读
      @GetMapping("/unread-count")    // 未读数量
  }
  
  // 消息类型
  - 系统通知
  - 课程更新通知
  - 订单通知
  - 学习提醒
  ```

**Day 5-7: 细节优化**
- [ ] **性能优化**
  ```bash
  1. 后端优化
     - Redis缓存热点数据
     - 数据库索引优化
     - SQL查询优化
     - 接口响应优化
  
  2. 前端优化
     - 组件懒加载
     - 图片懒加载
     - 首屏加载优化
     - 打包体积优化
  ```

- [ ] **用户体验优化**
  ```bash
  1. 加载状态优化
  2. 错误提示友好化
  3. 表单验证完善
  4. 操作反馈及时
  5. 移动端适配
  ```

**交付物**:
- ✅ AI服务功能完善
- ✅ 消息通知功能完成
- ✅ 系统性能优化完成

---

### 📅 第7周：测试与修复

#### Week 7: 全面测试（5-7天）

**Day 1-2: 单元测试**
- [ ] **后端单元测试**
  ```bash
  # 使用JUnit + Mockito
  
  1. UserService 测试
     - 注册功能测试
     - 登录功能测试
     - JWT生成测试
  
  2. CourseService 测试
     - CRUD操作测试
     - 搜索功能测试
     - 热门课程测试
  
  3. OrderService 测试
     - 订单创建测试
     - 支付流程测试
     - 订单状态测试
  
  4. LearningService 测试
     - 学习进度测试
     - 统计功能测试
  
  目标: 核心业务逻辑覆盖率 > 60%
  ```

**Day 3-4: 接口测试**
- [ ] **Postman/Apifox测试**
  ```bash
  1. 创建接口测试集合
  2. 编写测试用例
     - 正常场景测试
     - 异常场景测试
     - 边界值测试
  3. 自动化测试脚本
  4. 导出测试报告
  
  测试范围:
  - 用户服务: 10+ 接口
  - 课程服务: 30+ 接口
  - 订单服务: 15+ 接口
  - 学习服务: 15+ 接口
  - AI服务: 5+ 接口
  ```

**Day 5-6: 集成测试**
- [ ] **端到端测试场景**
  ```bash
  场景1: 用户注册到购买课程
    1. 用户注册
    2. 用户登录
    3. 浏览课程
    4. 查看课程详情
    5. 创建订单
    6. 支付订单
    7. 开始学习
  
  场景2: 用户学习流程
    1. 登录
    2. 进入课程学习
    3. 播放视频
    4. 记录学习进度
    5. 添加笔记
    6. 发表评论
    7. 使用AI助手
  
  场景3: 管理员管理流程
    1. 管理员登录
    2. 查看数据看板
    3. 审核课程
    4. 处理订单
    5. 管理用户
  ```

**Day 7: Bug修复**
- [ ] **问题汇总与修复**
  ```bash
  1. 收集测试问题
  2. 按优先级分类
     - P0: 阻塞性问题（必须修复）
     - P1: 严重问题（优先修复）
     - P2: 一般问题（尽量修复）
     - P3: 优化建议（可延后）
  3. 逐一修复验证
  4. 回归测试
  ```

**交付物**:
- ✅ 单元测试覆盖率达标
- ✅ 接口测试完成
- ✅ 集成测试通过
- ✅ 已知Bug修复完成

---

### 📅 第8周：部署上线

#### Week 8: 部署与上线（5-7天）

**Day 1-2: 服务器准备**
- [ ] **购买服务器**
  ```bash
  推荐配置:
  - 阿里云/腾讯云 学生服务器
  - 配置: 2核4G 或 4核8G
  - 系统: CentOS 7.9 或 Ubuntu 20.04
  - 带宽: 5M+
  ```

- [ ] **环境安装**
  ```bash
  # 1. 安装Java 17
  yum install java-17-openjdk-devel
  
  # 2. 安装MySQL 8
  # 导入数据库脚本
  
  # 3. 安装Redis
  yum install redis
  
  # 4. 安装Nginx
  yum install nginx
  
  # 5. 安装Docker（可选）
  curl -fsSL https://get.docker.com | bash -s docker
  ```

**Day 3-4: 应用部署**
- [ ] **后端部署**
  ```bash
  # 方案1: 直接部署
  # 1. 打包
  mvn clean package -DskipTests
  
  # 2. 上传jar包到服务器
  scp target/*.jar root@your-ip:/opt/xuetu/
  
  # 3. 启动服务
  nohup java -jar user-service.jar > user.log 2>&1 &
  nohup java -jar course-service.jar > course.log 2>&1 &
  nohup java -jar order-service.jar > order.log 2>&1 &
  nohup java -jar learning-service.jar > learning.log 2>&1 &
  nohup java -jar ai-service.jar > ai.log 2>&1 &
  nohup java -jar gateway.jar > gateway.log 2>&1 &
  
  # 方案2: Docker部署（推荐）
  # 编写Dockerfile
  # 构建镜像
  # docker-compose 启动
  ```

- [ ] **前端部署**
  ```bash
  # 1. 打包
  npm run build
  
  # 2. 上传到服务器
  scp -r dist/* root@your-ip:/usr/share/nginx/html/xuetu/
  
  # 3. 配置Nginx
  server {
      listen 80;
      server_name xuetu.com;
      
      location / {
          root /usr/share/nginx/html/xuetu;
          try_files $uri $uri/ /index.html;
      }
      
      location /api/ {
          proxy_pass http://localhost:8080/api/;
      }
  }
  
  # 4. 启动Nginx
  systemctl start nginx
  ```

**Day 5-6: 域名与HTTPS**
- [ ] **域名配置**
  ```bash
  1. 购买域名（可选）
  2. 域名解析到服务器IP
  3. 配置Nginx
  ```

- [ ] **HTTPS配置**
  ```bash
  # 使用Let's Encrypt免费证书
  1. 安装certbot
  yum install certbot python3-certbot-nginx
  
  2. 申请证书
  certbot --nginx -d xuetu.com
  
  3. 自动续期
  certbot renew --dry-run
  ```

**Day 7: 监控与优化**
- [ ] **监控配置**
  ```bash
  1. 日志管理
     - 配置日志滚动
     - 设置日志级别
     - 日志定期清理
  
  2. 系统监控（可选）
     - 安装Prometheus
     - 配置Grafana
     - 设置告警规则
  
  3. 应用监控
     - Spring Boot Actuator
     - 健康检查接口
  ```

- [ ] **性能优化**
  ```bash
  1. JVM参数优化
  java -Xms512m -Xmx1024m -jar app.jar
  
  2. 数据库优化
     - 索引优化
     - 慢查询分析
     - 连接池配置
  
  3. 缓存优化
     - Redis缓存策略
     - 缓存预热
     - 缓存过期策略
  ```

**交付物**:
- ✅ 所有服务部署成功
- ✅ 前端访问正常
- ✅ HTTPS配置完成
- ✅ 监控配置完成

---

## 📋 详细任务清单

### 🔴 高优先级（必须完成）

#### 后端核心
- [ ] AI服务完整开发（⭐⭐⭐⭐⭐）
- [ ] 用户角色权限管理（⭐⭐⭐⭐⭐）
- [ ] 修复所有TODO项（⭐⭐⭐⭐）
- [ ] JWT用户认证统一处理（⭐⭐⭐⭐）
- [ ] 文件上传功能（⭐⭐⭐⭐）
- [ ] 管理服务基础功能（⭐⭐⭐⭐）

#### 前端核心
- [ ] 用户端核心页面（⭐⭐⭐⭐⭐）
  - 首页
  - 登录注册
  - 课程列表
  - 课程详情
  - 学习页面
  - 个人中心
  - AI聊天
- [ ] 后台管理核心页面（⭐⭐⭐⭐）
  - 数据看板
  - 用户管理
  - 课程管理
  - 订单管理
  - 角色权限管理

#### 测试部署
- [ ] 接口测试（⭐⭐⭐⭐）
- [ ] 集成测试（⭐⭐⭐⭐）
- [ ] 服务器部署（⭐⭐⭐⭐⭐）

---

### 🟡 中优先级（重要但可延后）

- [ ] 单元测试（测试覆盖率60%+）
- [ ] 消息通知功能
- [ ] 数据统计报表
- [ ] 性能优化
- [ ] 日志监控
- [ ] API文档完善
- [ ] 移动端H5页面

---

### 🟢 低优先级（可选）

- [ ] 真实支付集成（支付宝/微信）
- [ ] 视频播放服务优化
- [ ] WebSocket实时通知
- [ ] 邮件通知功能
- [ ] 短信验证码
- [ ] 第三方登录（微信、QQ）
- [ ] 搜索引擎优化（SEO）
- [ ] 移动端APP

---

## 🧪 测试策略

### 1. 单元测试
```bash
目标: 核心业务逻辑覆盖率 > 60%

工具: JUnit 5 + Mockito

重点测试:
- Service层业务逻辑
- 工具类方法
- 关键算法

示例:
@Test
void testUserRegister() {
    // Given
    UserRegisterRequest request = new UserRegisterRequest();
    request.setUsername("test");
    request.setPassword("123456");
    
    // When
    Long userId = userService.register(request);
    
    // Then
    assertNotNull(userId);
    assertTrue(userId > 0);
}
```

### 2. 接口测试
```bash
工具: Postman / Apifox

测试用例:
- 正常场景: 参数正确，返回成功
- 异常场景: 参数错误，返回错误信息
- 边界场景: 最大值、最小值、空值
- 权限场景: 无权限访问，返回403

自动化:
- 编写Pre-request Script
- 编写Tests脚本
- 环境变量管理
- 批量运行
```

### 3. 集成测试
```bash
工具: Spring Boot Test + TestRestTemplate

测试流程:
1. 启动测试环境
2. 执行端到端场景
3. 验证数据一致性
4. 清理测试数据

示例场景:
用户注册 → 登录 → 购买课程 → 学习 → 评论
```

### 4. 性能测试（可选）
```bash
工具: JMeter / Gatling

测试场景:
- 并发登录测试（100用户）
- 课程列表查询（200 QPS）
- 订单创建压测（50 QPS）

性能指标:
- 响应时间 < 500ms
- 错误率 < 1%
- TPS > 100
```

---

## 📊 进度管理

### 周报制度
```markdown
每周五总结：
1. 本周完成的任务
2. 遇到的问题和解决方案
3. 下周计划
4. 风险提示
```

### 里程碑检查
```bash
Week 2: 后端核心功能完成检查
Week 4: 前端用户端完成检查
Week 6: 全部功能开发完成检查
Week 7: 测试完成检查
Week 8: 上线成功检查
```

---

## 🚨 风险控制

### 风险识别
| 风险 | 可能性 | 影响 | 应对策略 |
|------|--------|------|---------|
| AI API不稳定 | 中 | 高 | 准备多个AI服务备选 |
| 前端开发延期 | 中 | 中 | 使用现成模板加速 |
| 测试时间不足 | 高 | 中 | 开发时同步测试 |
| 服务器部署问题 | 低 | 高 | 提前准备Docker方案 |
| 时间不够 | 中 | 高 | 功能裁剪，保证核心 |

### 应急预案
```bash
1. AI服务开发困难
   → 简化功能，只实现基础问答
   → 使用现成的开源AI对话框架

2. 前端开发延期
   → 使用现成的Admin模板
   → 减少定制化，使用默认样式

3. 测试时间不足
   → 优先手工测试核心功能
   → 单元测试可以延后

4. 部署困难
   → 使用Docker简化部署
   → 寻求运维帮助
```

---

## 💡 成功关键

### 1. 专注核心功能
```bash
✅ 必须有: AI问答、课程学习、基础管理
⏸️ 可以晚点: 完善的测试、华丽的UI
❌ 不必要: 过度设计、非核心功能
```

### 2. 快速迭代
```bash
1. 先实现基础功能
2. 快速测试验证
3. 收集反馈
4. 迭代优化
```

### 3. 时间分配
```bash
后端开发: 35% (2.8周)
前端开发: 30% (2.4周)
测试修复: 20% (1.6周)
部署上线: 15% (1.2周)
```

### 4. 质量保证
```bash
1. 代码规范: 使用统一的代码风格
2. 代码审查: 关键代码自我审查
3. 文档完善: 接口文档和README
4. 异常处理: 统一的错误处理机制
```

---

## 🎯 验收标准

### 后端验收
- [ ] 所有核心接口开发完成
- [ ] 接口文档完整（Knife4j）
- [ ] 核心功能测试通过
- [ ] 无阻塞性Bug
- [ ] 代码规范、注释完整

### 前端验收
- [ ] 所有核心页面开发完成
- [ ] 页面功能正常
- [ ] 响应式布局正常
- [ ] 兼容主流浏览器
- [ ] 用户体验良好

### 部署验收
- [ ] 所有服务启动正常
- [ ] 前端访问正常
- [ ] 数据库连接正常
- [ ] 域名解析正常（如有）
- [ ] HTTPS配置成功（如有）

---

## 📚 参考资源

### 技术文档
- Spring Boot官方文档: https://spring.io/projects/spring-boot
- Vue 3官方文档: https://cn.vuejs.org/
- Element Plus: https://element-plus.org/
- 百度AI平台: https://ai.baidu.com/

### 开源项目参考
- RuoYi-Vue: https://gitee.com/y_project/RuoYi-Vue
- Vue Pure Admin: https://github.com/pure-admin/vue-pure-admin
- Spring Cloud Alibaba: https://github.com/alibaba/spring-cloud-alibaba

---

## 🎓 总结

### 本规划特点
✅ **完整**: 覆盖后端、前端、测试、部署全流程  
✅ **实用**: 基于项目实际情况制定  
✅ **可执行**: 任务分解到天，可跟踪执行  
✅ **有重点**: 明确优先级，聚焦核心功能  
✅ **有弹性**: 提供应急预案和裁剪策略  

### 执行建议
1. **严格按周计划执行**，每周五自查进度
2. **遇到问题及时调整**，不要死磕难点
3. **保持代码质量**，不要为赶进度降低质量
4. **多沟通多请教**，遇到技术难题寻求帮助
5. **记录开发过程**，为简历和面试积累素材

### 预期成果
8周后，你将拥有：
- ✅ 一个功能完整的在线教育平台
- ✅ 集成AI功能的核心亮点
- ✅ 前后端分离的完整项目经验
- ✅ 微服务架构的实战经验
- ✅ 可以部署上线的真实项目
- ✅ 一份有竞争力的简历项目

---

**🚀 准备好了吗？从修复现有问题和AI服务开发开始吧！**
