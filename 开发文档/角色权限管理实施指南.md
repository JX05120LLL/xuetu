# 🔐 角色权限管理 - 实施指南

> 完成时间: 2025年10月2日  
> 状态: ✅ 开发完成，待测试

---

## 📋 目录
1. [开发完成清单](#开发完成清单)
2. [部署步骤](#部署步骤)
3. [测试指南](#测试指南)
4. [使用说明](#使用说明)

---

## ✅ 开发完成清单

### 第1步：数据库初始化 ✅

**文件**: `sql/rbac_init_data.sql`

**内容**:
- ✅ 5个角色（SUPER_ADMIN, ADMIN, TEACHER, VIP_MEMBER, STUDENT）
- ✅ 37个权限（课程、订单、学习、AI、用户、系统管理等）
- ✅ 完整的角色权限关系配置
- ✅ 4个测试账号

**测试账号**:
| 用户名 | 密码 | 角色 | 说明 |
|--------|------|------|------|
| admin | admin123 | SUPER_ADMIN | 超级管理员 |
| teacher | teacher123 | TEACHER | 讲师 |
| vip | vip123 | VIP_MEMBER | VIP会员 |
| (已有用户ID=1) | - | STUDENT | 普通学生 |

---

### 第2步：Admin Service 角色权限管理 ✅

**新增文件** (18个):

**实体类** (4个):
- `com.star.admin.entity.Role`
- `com.star.admin.entity.Permission`
- `com.star.admin.entity.RolePermission`
- `com.star.admin.entity.UserRole`

**DTO** (4个):
- `com.star.admin.dto.RoleDTO`
- `com.star.admin.dto.PermissionDTO`
- `com.star.admin.dto.AssignRoleRequest`
- `com.star.admin.dto.GrantPermissionRequest`

**Mapper** (4个):
- `com.star.admin.mapper.RoleMapper`
- `com.star.admin.mapper.PermissionMapper`
- `com.star.admin.mapper.RolePermissionMapper`
- `com.star.admin.mapper.UserRoleMapper`

**Service** (4个):
- `com.star.admin.service.RoleService`
- `com.star.admin.service.impl.RoleServiceImpl`
- `com.star.admin.service.PermissionService`
- `com.star.admin.service.impl.PermissionServiceImpl`

**Controller** (2个):
- `com.star.admin.controller.RoleController`
- `com.star.admin.controller.PermissionController`

**API接口** (14个):
```bash
# 角色管理 (9个)
GET    /role/list              # 获取所有角色
GET    /role/{id}              # 获取角色详情
POST   /role/create            # 创建角色
PUT    /role/update            # 更新角色
DELETE /role/{id}              # 删除角色
GET    /role/user/{userId}     # 获取用户角色
POST   /role/assign            # 分配角色
POST   /role/grant             # 授予权限
GET    /role/{id}/permissions  # 获取角色权限

# 权限管理 (5个)
GET    /permission/list                # 获取所有权限
GET    /permission/{id}                # 获取权限详情
POST   /permission/create              # 创建权限
PUT    /permission/update              # 更新权限
DELETE /permission/{id}                # 删除权限
GET    /permission/user/{userId}/keys  # 获取用户权限Key
```

---

### 第3步：User Service 登录逻辑修改 ✅

**修改的文件** (4个):

1. **LoginResponse.java** - 添加roles和permissions字段
```java
private List<String> roles;        // 🆕 用户角色列表
private List<String> permissions;  // 🆕 用户权限列表
```

2. **UserLoginResponse.java** - 添加permissions字段
```java
private List<String> permissions;  // 🆕 用户权限列表
```

3. **新增Feign客户端** (2个):
- `com.star.user.feign.AdminServiceClient` - 调用admin-service
- `com.star.user.feign.RoleDTO` - 角色DTO

4. **UserServiceImpl.java** - 修改登录逻辑
```java
// 🆕 调用admin-service获取角色和权限
R<List<RoleDTO>> rolesResult = adminServiceClient.getUserRoles(user.getId());
R<List<String>> permsResult = adminServiceClient.getUserPermissionKeys(user.getId());

// 🆕 生成包含角色权限的JWT Token
Map<String, Object> claims = new HashMap<>();
claims.put("userId", user.getId());
claims.put("username", user.getUsername());
claims.put("roles", roles);
claims.put("permissions", permissions);

String token = JwtUtil.generateToken(claims, expireTime);
```

---

### 第4步：Gateway 权限验证过滤器 ✅

**新增文件** (2个):

1. **PermissionConfig.java** - 权限配置
```java
// URL权限映射
/api/course/create  → [TEACHER, ADMIN, SUPER_ADMIN]
/api/order/all      → [ADMIN, SUPER_ADMIN]
/api/role/**        → [ADMIN, SUPER_ADMIN]

// 白名单
/api/user/auth/login
/api/user/auth/register
/api/course/list
...
```

2. **AuthorizationFilter.java** - 权限验证过滤器
```java
public class AuthorizationFilter implements GlobalFilter, Ordered {
    @Override
    public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {
        // 1. 白名单检查
        // 2. 解析JWT获取角色
        // 3. 验证URL权限
        // 4. 传递用户信息给下游
    }
    
    @Override
    public int getOrder() {
        return 2;  // 在JWT验证(Order=1)之后执行
    }
}
```

**修改的文件** (1个):
- `JwtAuthFilter.java` - Order从-100改为1

---

## 🚀 部署步骤

### 步骤1：执行数据库初始化 ⭐

```bash
# 进入SQL目录
cd sql

# 执行初始化脚本
mysql -u root -p xuetu_db < rbac_init_data.sql

# 输入密码后，查看初始化结果
# 应该显示：
# - 5个角色
# - 37个权限
# - 角色权限关系
# - 4个测试用户
```

**验证**:
```sql
-- 查看角色
SELECT * FROM role;

-- 查看权限数量
SELECT COUNT(*) FROM permission;

-- 查看测试用户
SELECT u.username, r.role_name 
FROM user u 
JOIN user_role ur ON u.id = ur.user_id 
JOIN role r ON ur.role_id = r.id 
WHERE u.id >= 100;
```

---

### 步骤2：启动服务 ⭐

**启动顺序**:

1. **Nacos** (如果未启动)
```bash
startup.cmd -m standalone
```

2. **Admin Service** (端口: 8055)
```bash
cd admin-service
mvn spring-boot:run
```

等待启动成功，访问: http://localhost:8055/doc.html

3. **User Service** (端口: 8033)
```bash
cd user-service
mvn spring-boot:run
```

4. **Gateway** (端口: 8080)
```bash
cd gateway
mvn spring-boot:run
```

5. **其他服务** (可选，用于完整测试)
```bash
# Course Service (8044)
cd course-service
mvn spring-boot:run

# Order Service (8022)
cd order-service
mvn spring-boot:run
```

---

## 🧪 测试指南

### 测试1：登录获取角色和权限 ⭐⭐⭐⭐⭐

#### 1.1 管理员登录
```bash
POST http://localhost:8080/api/user/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "admin123"
}
```

**期望响应**:
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "tokenType": "Bearer",
    "expiresIn": 604800,
    "userInfo": {
      "id": 100,
      "username": "admin",
      "nickname": "系统管理员",
      "email": "admin@xuetu.com",
      "avatar": null,
      "roles": ["SUPER_ADMIN"]  // ✅ 角色列表
    },
    "permissions": [            // ✅ 权限列表
      "course:create",
      "course:update",
      ...
    ]
  }
}
```

#### 1.2 讲师登录
```bash
POST http://localhost:8080/api/user/auth/login
{
  "username": "teacher",
  "password": "teacher123"
}
```

**期望**: roles包含 `TEACHER`

#### 1.3 VIP会员登录
```bash
POST http://localhost:8080/api/user/auth/login
{
  "username": "vip",
  "password": "vip123"
}
```

**期望**: roles包含 `VIP_MEMBER`

---

### 测试2：权限验证 ⭐⭐⭐⭐⭐

#### 2.1 学生尝试创建课程（应该失败）

```bash
# 1. 先用学生账号登录（假设已有学生账号）
POST http://localhost:8080/api/user/auth/login
{
  "username": "student",
  "password": "student123"
}

# 2. 获取token后，尝试创建课程
POST http://localhost:8080/api/course/create
Authorization: Bearer <student_token>
{
  "title": "测试课程"
}
```

**期望响应**: 403 Forbidden
```json
{
  "code": 403,
  "message": "权限不足，无法访问该资源",
  "data": null
}
```

#### 2.2 讲师创建课程（应该成功）

```bash
# 1. 用讲师账号登录
POST http://localhost:8080/api/user/auth/login
{
  "username": "teacher",
  "password": "teacher123"
}

# 2. 创建课程
POST http://localhost:8080/api/course/create
Authorization: Bearer <teacher_token>
{
  "title": "Java入门课程",
  "categoryId": 1,
  "description": "适合零基础学员",
  "price": 99.00
}
```

**期望响应**: 200 OK ✅

#### 2.3 学生查看订单（应该失败）

```bash
# 学生尝试查看所有订单
GET http://localhost:8080/api/order/all
Authorization: Bearer <student_token>
```

**期望响应**: 403 Forbidden

#### 2.4 管理员查看订单（应该成功）

```bash
GET http://localhost:8080/api/order/all
Authorization: Bearer <admin_token>
```

**期望响应**: 200 OK ✅

---

### 测试3：角色管理API ⭐⭐⭐⭐

#### 3.1 查看所有角色
```bash
GET http://localhost:8080/api/role/list
Authorization: Bearer <admin_token>
```

**期望响应**:
```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "roleName": "SUPER_ADMIN",
      "description": "超级管理员",
      "permissions": [...],
      "permissionIds": [1, 2, 3, ...]
    },
    // ... 其他角色
  ]
}
```

#### 3.2 为用户分配角色
```bash
POST http://localhost:8080/api/role/assign
Authorization: Bearer <admin_token>
{
  "userId": 2,
  "roleId": 3
}
```

**期望**: 用户ID=2被分配讲师角色

#### 3.3 为角色授予权限
```bash
POST http://localhost:8080/api/role/grant
Authorization: Bearer <admin_token>
{
  "roleId": 5,
  "permissionIds": [1, 2, 3, 6, 8, 9, 12, 13, 16, 17, 32]
}
```

**期望**: 角色ID=5(STUDENT)获得指定权限

---

### 测试4：白名单验证 ⭐⭐⭐

#### 4.1 未登录访问课程列表（应该成功）
```bash
GET http://localhost:8080/api/course/list
# 不需要Authorization
```

**期望响应**: 200 OK ✅

#### 4.2 未登录访问创建课程（应该失败）
```bash
POST http://localhost:8080/api/course/create
# 不需要Authorization
{
  "title": "测试"
}
```

**期望响应**: 401 Unauthorized

---

## 📖 使用说明

### 1. 前端调用示例

```javascript
// 1. 登录
const login = async () => {
  const response = await axios.post('/api/user/auth/login', {
    username: 'admin',
    password: 'admin123'
  });
  
  const { token, roles, permissions } = response.data.data;
  
  // 保存到localStorage
  localStorage.setItem('token', token);
  localStorage.setItem('roles', JSON.stringify(roles));
  localStorage.setItem('permissions', JSON.stringify(permissions));
};

// 2. 请求拦截器 - 添加Token
axios.interceptors.request.use(config => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 3. 权限判断
const hasPermission = (permission) => {
  const permissions = JSON.parse(localStorage.getItem('permissions') || '[]');
  return permissions.includes(permission);
};

const hasRole = (role) => {
  const roles = JSON.parse(localStorage.getItem('roles') || '[]');
  return roles.includes(role);
};

// 4. 使用示例
if (hasRole('TEACHER') || hasRole('ADMIN')) {
  // 显示创建课程按钮
}

if (hasPermission('course:delete')) {
  // 显示删除按钮
}
```

---

### 2. 后端服务获取用户信息

```java
// Controller中获取用户ID和角色
@GetMapping("/my-info")
public R<UserInfo> getMyInfo(HttpServletRequest request) {
    // Gateway会将用户信息放入Header
    String userId = request.getHeader("X-User-Id");
    String username = request.getHeader("X-Username");
    String roles = request.getHeader("X-User-Roles");  // 逗号分隔
    
    // 业务逻辑
    return R.ok(userInfo);
}
```

---

### 3. 添加新的权限规则

**在PermissionConfig.java中添加**:

```java
// 新增URL权限
URL_PERMISSIONS.put("/api/your-service/admin", Arrays.asList("ADMIN", "SUPER_ADMIN"));

// 添加白名单
WHITE_LIST.add("/api/your-service/public");
```

---

## 🔧 故障排查

### 问题1：登录后没有角色和权限

**原因**: admin-service未启动或未执行SQL初始化

**解决**:
```bash
# 1. 检查admin-service是否启动
curl http://localhost:8055/actuator/health

# 2. 检查数据库
mysql> SELECT COUNT(*) FROM user_role;  # 应该有记录

# 3. 重新执行SQL
mysql -u root -p xuetu_db < sql/rbac_init_data.sql
```

---

### 问题2：403权限不足

**原因**: 用户角色不匹配URL要求

**解决**:
```bash
# 1. 查看用户的角色
GET /api/role/user/{userId}

# 2. 查看URL需要的角色
# 在PermissionConfig.java中查找

# 3. 为用户分配正确的角色
POST /api/role/assign
{
  "userId": 1,
  "roleId": 3
}
```

---

### 问题3：Token解析失败

**原因**: JWT工具类不支持带claims的生成

**解决**: 确保JwtUtil.generateToken支持Map参数
```java
public static String generateToken(Map<String, Object> claims, long expireTime) {
    return Jwts.builder()
        .setClaims(claims)
        .setExpiration(new Date(System.currentTimeMillis() + expireTime))
        .signWith(SignatureAlgorithm.HS256, SECRET_KEY)
        .compact();
}
```

---

## 🎉 总结

### 已实现的功能
- ✅ 完整的RBAC角色权限模型
- ✅ 5种角色（超级管理员、管理员、讲师、VIP、学生）
- ✅ 37个细粒度权限
- ✅ 登录返回角色和权限
- ✅ 网关统一权限验证
- ✅ 角色管理API
- ✅ 权限管理API

### 安全特性
- ✅ JWT Token携带角色权限
- ✅ URL级别权限控制
- ✅ 白名单机制
- ✅ 统一异常处理

### 下一步建议
1. ✅ 测试所有功能
2. 📱 开发前端权限控制
3. 🔍 添加操作日志
4. 📊 权限使用统计

---

**🎉 恭喜！角色权限管理系统开发完成！**

**现在可以开始测试了！** 🚀
